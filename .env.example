PORT=3000
PUBLIC_URL=http://localhost:3000
DATABASE_FILE=./data/kareone.sqlite
JWT_SECRET=replace-this-with-a-long-random-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=KareOne@2026!

# Optional SMTP configuration. If omitted, the admin panel still creates a quote link
# but the send endpoint reports that email delivery is not configured.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM="KareOne <hello@kareone.ir>"
